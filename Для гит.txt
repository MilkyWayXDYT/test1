#include <iostream>
#include <stdio.h>
#include <string.h>

using namespace std;

int main() {
    
    double x,y;
    char first;
    cout << "Enter your name";
    cin >> first;
    cout << "Hello, "<< first <<endl;
    cout << "Enter your coordinates:"<<endl;
    cin >> x;
    cin >> y;
    if(((y < 0.3) && (x*x + y * y > 1))||((y < -0.3) && (x*x + y * y < -1)))
    cout<<"The point lies within the area";
    else (((x*x + y * y < 1)||((y > 0.3) && (x*x + y * y > 1)))||((x*x + y * y < 1) || ((y < -0.3) && (x*x + y * y < -1))))
    cout<<"The point lies outside the area";
    else
    cout << "The pont lies on the edge of the area."
    cout << "I hope that we helped you.";
}
